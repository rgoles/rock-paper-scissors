{
  "devDependencies": {
    "browser-sync": "^2.26.14"
  },
  "scripts": {
    "watch": "browser-sync start --config bs-config.js",
    "test": "browser-sync start --server --directory --files \"*\""
  }
}
